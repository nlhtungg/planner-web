# Frontend application environment

VITE_GOOGLE_CLIENT_ID=your-google-client-id

# Same-origin defaults work behind both the Vite dev proxy and the production Nginx edge.
VITE_API_URL=/api
VITE_SOCKET_URL=
